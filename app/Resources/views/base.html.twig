<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta http-equiv="X-UA-Compatible" content="IE=edge" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>{{ title }}</title>

<!-- Bootstrap -->
<link href="{{ asset('css/bootstrap.min.css') }}" rel="stylesheet" />
<link href="{{ asset('css/bootstrap-theme.min.css') }}" rel="stylesheet" />
<!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
<!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
<!--[if lt IE 9]>
  <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
  <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
<![endif]-->

<link href="{{ asset('css/styles.css') }}" rel="stylesheet" />

<link href="{{ asset('favicon.png') }}" type="image/png" rel="icon" />

</head>
<body>

<div class="headnav bg-primary">
    <div class="container">
        <nav>
            <!-- Avoid inserting spaces between <a> !! -->
            <a href="{{ path('blog') }}" {% if navi == 'blog' %} class="active" {% endif %}>Blog</a
                ><a href="{{ path('about') }}" {% if navi == 'about' %} class="active" {% endif %}>About</a
                ><a href="{{ path('get-it') }}" {% if navi == 'getIt' %} class="active" {% endif %}>Get It!</a
                ><a href="{{ path('contribute') }}" {% if navi == 'contribute' %} class="active" {% endif %}>Contribute!</a>
            
          {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <a href="{{ path('logout') }}" title="Logout" class="pull-right">
                <span class="glyphicon glyphicon glyphicon-off" aria-hidden="true"></span>
                <span class="sr-only">Logout</span>
            </a>
          {% endif %}
        </nav>
    </div>
</div>

{% block precontent %}{% endblock %}

<div class="container">
    
    <div class="row">
        
        <div class="col-md-8">
            
            {% for flashMessage in app.session.flashbag.get('success') %}
                <div class="alert alert-success alert-dismissible" role="alert">
                    <button class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    {{ flashMessage|markdown }}
                </div>
            {% endfor %}
            {% for flashMessage in app.session.flashbag.get('info') %}
                <div class="alert alert-info alert-dismissible" role="alert">
                    <button class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    {{ flashMessage|markdown }}
                </div>
            {% endfor %}
            {% for flashMessage in app.session.flashbag.get('warning') %}
                <div class="alert alert-warning alert-dismissible" role="alert">
                    <button class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    {{ flashMessage|markdown }}
                </div>
            {% endfor %}
            {% for flashMessage in app.session.flashbag.get('danger') %}
                <div class="alert alert-danger alert-dismissible" role="alert">
                    <button class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    {{ flashMessage|markdown }}
                </div>
            {% endfor %}
            
            <div class="page-header">
                <h1>{{ title }}</h1>
            </div>
            
            {% block content %}{% endblock %}
            
        </div>
        
        <div class="col-md-4 sidebar">
          
          {% block sidebar %}
                
            {{ render(controller('AppBundle:Sidebar:recentPosts')) }}
            
            {{ render(controller('AppBundle:Sidebar:blogTags')) }}
        
          {% endblock %}
            
        </div>
        
    </div>
    
</div>

<script src="{{ asset('js/jquery-1.11.2.min.js') }}"></script>
<script src="{{ asset('js/bootstrap.min.js') }}"></script>
</body>
</html>
