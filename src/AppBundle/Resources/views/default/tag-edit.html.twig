{% extends 'AppBundle::base.html.twig' %}

{% block content %}

{{ form_start(form) }}
    {{ form_errors(form) }}
    <div class="form-group">
        {{ form_row(form.title) }}
    </div>
    <div class="form-group">
        {{ form_widget(form.save) }}
        {{ form_widget(form.delete) }}
    </div>
{{ form_end(form) }}

{% endblock %}
